// Language packs, keyed the same way as the data modules (hero, family,
// timeline, saveTheDate, location, footer, ui). Marathi is the base
// content of the theme itself, so it has no pack.
import * as hi from "./hi";
import * as en from "./en";

export const PACKS = { hi, en };
